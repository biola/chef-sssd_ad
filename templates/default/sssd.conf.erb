[sssd]
services = nss, pam
config_file_version = 2
domains = <%= node['samba']['options']['realm'].upcase %>

[domain/<%= node['samba']['options']['realm'].upcase %>]
id_provider = ad
access_provider = ad

# Use /home/DOMAIN-FQDN/user as $HOME
override_homedir = /home/%d/%u
